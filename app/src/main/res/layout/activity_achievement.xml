<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_content_bg"
    android:orientation="vertical">

    <include layout="@layout/action_bar" />

    <TextView
        android:id="@+id/achievement_title"
        android:layout_width="match_parent"
        android:layout_height="@dimen/widget_size_44"
        android:textColor="@color/white"
        android:textSize="@dimen/font_size_14"
        android:paddingLeft="@dimen/widget_size_10"
        android:text="The existing competitive staff:100"
        android:gravity="center_vertical"
        android:background="#5380F4"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <com.cheng.refresh.library.PullToRefreshListView
            android:id="@+id/refresh_listView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:listSelector="@android:color/transparent"/>

        <RelativeLayout
            android:id="@+id/empty_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:drawablePadding="@dimen/widget_size_16"
                android:gravity="center_horizontal"
                android:text="No Achievement Record"
                android:textColor="#999999" />
        </RelativeLayout>
    </FrameLayout>
</LinearLayout>